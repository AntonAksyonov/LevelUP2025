package homework7.atomobile;

public class Car extends Automobile {

    public Car(String number, int travelSpeed, int weight, float width, float height, float length) {
        super(TypeAutomobile.CAR, number, travelSpeed, weight, width, height, length);
    }
}
